version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./student_tracker.db:/app/student_tracker.db
    environment:
      - SECRET_KEY=change-this-in-production
      - DB_PATH=student_tracker.db